import React from 'react';

// FoodHungerScatter has been deprecated in favor of the Inequality Timeline.
// We keep a minimal placeholder to avoid import errors in older branches.
export default function FoodHungerScatter() {
  return null;
}
